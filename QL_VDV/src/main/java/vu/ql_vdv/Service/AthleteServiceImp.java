package vu.ql_vdv.Service;

/*import org.springframework.data.domain.Example;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.repository.query.FluentQuery;
import org.springframework.stereotype.Service;
import vu.ql_vdv.Entity.Athlete;
import vu.ql_vdv.Repository.AthleteRepository;

import java.util.List;
import java.util.Optional;
import java.util.function.Function;

@Service
public class AthleteServiceImp implements AthleteService{
    AthleteRepository athleteRepository;
    public AthleteServiceImp(AthleteRepository athleteRepository){
        this.athleteRepository = athleteRepository;
    }

    public List<Athlete> findAll() {
        return athleteRepository.findAll();
    }

    public List<Athlete> findAllById(Iterable<Long> longs) {
        return athleteRepository.findAllById(longs);
    }

    public <S extends Athlete> List<S> saveAll(Iterable<S> entities) {
        return athleteRepository.saveAll(entities);
    }

    public void flush() {
        athleteRepository.flush();
    }

    public <S extends Athlete> S saveAndFlush(S entity) {
        return athleteRepository.saveAndFlush(entity);
    }

    public <S extends Athlete> List<S> saveAllAndFlush(Iterable<S> entities) {
        return athleteRepository.saveAllAndFlush(entities);
    }

    public void deleteAllInBatch(Iterable<Athlete> entities) {
        athleteRepository.deleteAllInBatch(entities);
    }

    public void deleteAllByIdInBatch(Iterable<Long> longs) {
        athleteRepository.deleteAllByIdInBatch(longs);
    }

    public void deleteAllInBatch() {
        athleteRepository.deleteAllInBatch();
    }

    @Deprecated
    public Athlete getById(Long aLong) {
        return athleteRepository.getById(aLong);
    }

    public Athlete getReferenceById(Long aLong) {
        return athleteRepository.getReferenceById(aLong);
    }

    public <S extends Athlete> List<S> findAll(Example<S> example) {
        return athleteRepository.findAll(example);
    }

    public Page<Athlete> findAll(Pageable pageable) {
        return athleteRepository.findAll(pageable);
    }

    public <S extends Athlete> S save(S entity) {
        return athleteRepository.save(entity);
    }

    public Optional<Athlete> findById(Long aLong) {
        return athleteRepository.findById(aLong);
    }

    public boolean existsById(Long aLong) {
        return athleteRepository.existsById(aLong);
    }

    public long count() {
        return athleteRepository.count();
    }

    public void deleteById(Long aLong) {
        athleteRepository.deleteById(aLong);
    }

    public void delete(Athlete entity) {
        athleteRepository.delete(entity);
    }

    public void deleteAllById(Iterable<? extends Long> longs) {
        athleteRepository.deleteAllById(longs);
    }

    public void deleteAll(Iterable<? extends Athlete> entities) {
        athleteRepository.deleteAll(entities);
    }

    public void deleteAll() {
        athleteRepository.deleteAll();
    }

    public <S extends Athlete> long count(Example<S> example) {
        return athleteRepository.count(example);
    }

    public <S extends Athlete> boolean exists(Example<S> example) {
        return athleteRepository.exists(example);
    }

    public <S extends Athlete, R> R findBy(Example<S> example, Function<FluentQuery.FetchableFluentQuery<S>, R> queryFunction) {
        return athleteRepository.findBy(example, queryFunction);
    }
}*/
